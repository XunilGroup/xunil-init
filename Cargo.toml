[package]
name = "init"
version = "0.1.0"
edition = "2024"

[dependencies]
xunil = { package = "libxunil", path = "../libxunil" }

[profile.release]
panic = "abort"
opt-level = "s"

[profile.dev]
panic = "abort"
